name: Toy Example Workflow

on:
  push:
    branches:
      - main

jobs:
  print-message:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Print Hello World
        run: echo "Hello, GitHub Actions is working!"
